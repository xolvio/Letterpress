dependencies:
  override:
    - curl https://install.meteor.com | sh

test:
  override:
    # Run meteor in Velocity test mode and capture all the artifacts
    - meteor --test:
        environment:
          VELOCITY_CI: true